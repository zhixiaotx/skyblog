<?xml version="1.0" encoding="utf-8"?>
<search> 
  
  
    
    <entry>
      <title>关于-GitHub-的一些事情</title>
      <link href="/skyblog/2026/08/27/%E5%85%B3%E4%BA%8E-GitHub-%E7%9A%84%E4%B8%80%E4%BA%9B%E4%BA%8B%E6%83%85/"/>
      <url>/skyblog/2026/08/27/%E5%85%B3%E4%BA%8E-GitHub-%E7%9A%84%E4%B8%80%E4%BA%9B%E4%BA%8B%E6%83%85/</url>
      
        <content type="html"><![CDATA[<h1 id="关于-GitHub-的一些事情"><a href="#关于-GitHub-的一些事情" class="headerlink" title="关于 GitHub 的一些事情"></a>关于 GitHub 的一些事情</h1><blockquote><p>本篇文章探讨了关于 GitHub 的一些事情。作者提到了 GitHub 上一个项目可以有多种下载到本地的方式，包括在线克隆 fork 到自己仓库、通过链接下载克隆到本地文件夹以及下载 ZIP 解压到本地文件等方法。然后，作者分享了一些遇到的问题和解决方法，例如如何绑定多个 GitHub 账号以及本地怎么部署大文件到 GitHub 上去等。最后，作者总结道：<strong>适合自己才是最好的</strong>，还有许多未知未解之谜等待我们去发掘。</p><p>⚠️ <em>It has been 853 days since the last update, the content of the article may be outdated.</em></p></blockquote><hr><h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>事情是这样的，最近因为在研究几个项目，准备搭建各种关于个人的主页和模板，开始踏入我的代码研究以及 GitHub 部署等征途。大概今天的任务就是为了 fork 大佬的一个模板，但是因为发现有很多代码需要修改，加之在线修改很难去操作等，所以直接从 GitHub 上下载出来，但是在后面遇到了一系列的困难，所以发现还是有很多要学的东西！</p><hr><h2 id="GitHub-项目下载方式"><a href="#GitHub-项目下载方式" class="headerlink" title="GitHub 项目下载方式"></a>GitHub 项目下载方式</h2><p>从目前对 GitHub 的了解，一个项目可以有多种下载到本地的方式：</p><h3 id="方式一：在线克隆-Fork-到自己仓库"><a href="#方式一：在线克隆-Fork-到自己仓库" class="headerlink" title="方式一：在线克隆 Fork 到自己仓库"></a>方式一：在线克隆 Fork 到自己仓库</h3><p>这种方式——直接在线 fork 一下，对于开源的仓库，我们都是可以看得到，所以直接到原作者或者二改开源作者那里一键 fork 到自己的仓库就可以。但是此种方法有很多限制，例如：有 fork 标记，且各种别人的仓库信息挂在自己的仓库上，显得非常不美观，且感觉就是偷来的东西，而且还被别人打上了标签。因此，实在是无法操作可能，<strong>不推荐此种方法！</strong></p><ul><li><strong>优点</strong>：方便快捷，克隆速度快，不用再次部署到 GitHub，相当于直接备份！</li><li><strong>缺点</strong>：复制克隆生搬硬套，不好修改，容易出现问题，不方便了解项目的具体内容。</li></ul><h3 id="方式二：通过链接下载克隆到本地文件夹"><a href="#方式二：通过链接下载克隆到本地文件夹" class="headerlink" title="方式二：通过链接下载克隆到本地文件夹"></a>方式二：通过链接下载克隆到本地文件夹</h3><p>首先，找到想要的项目开源代码主页，找到 <code>Code</code> 选项下的 HTTP 或者 SSH，选择第一排或者第二排等，直接点击 <code>git clone</code> 然后复制，再到本地创建一个新的文件夹，直接打开 <strong>Git Bash Here</strong>，然后把刚才的项目地址直接粘贴运行，跑程序式克隆项目到本地即可。</p><ul><li><strong>优点</strong>：较方便快捷，可修改程度高，了解代码项目的具体内容更加方便快捷！</li><li><strong>缺点</strong>：部署到其他 Git 平台不方便，且容易丢失文件，且项目的部署方式麻烦，每次修改都得部署一次！</li></ul><h3 id="方式三：下载-ZIP-解压到本地文件"><a href="#方式三：下载-ZIP-解压到本地文件" class="headerlink" title="方式三：下载 ZIP 解压到本地文件"></a>方式三：下载 ZIP 解压到本地文件</h3><p>还是找到想要的项目开源代码主页，到 <code>Code</code> 栏中，下面几列，直接打包下载 ZIP 文件，然后解压到本地文件夹里。注意一下，这种方式下载还是限制会比较大，因为网速和国内访问速度差异，有时候下载会非常慢，甚至卡断。因此这种方式下载，最好选择一些镜像服务下载 ZIP。而这种方式是相比于第一种更加直接，相对于第二种，很多项目会提供下载镜像，下载方便。</p><ul><li><strong>优点</strong>：下载方便，一键下载，本地解压，速度也较快。</li><li><strong>缺点</strong>：没有镜像服务，基本下载几 KB&#x2F;s，而且容易下载出问题。</li></ul><hr><h2 id="新手实操问题"><a href="#新手实操问题" class="headerlink" title="新手实操问题"></a>新手实操问题</h2><p>再接下来，就是我遇到的一些问题了，一些关于 GitHub 相对于”小白”新手，真正难的实操问题：</p><p><strong>Q1</strong>：一台电脑怎么绑定多个 GitHub 账号？怎么好像只能生成一个 SSH Key？</p><p><strong>W1</strong>：由于前面发现，一个 GitHub 账号只能提供一个静态的域名 Page 服务，所以还想要再白嫖一个永久的域名 Page，然后把以后的工作转移到这个网站上。——（此方法已经由 ChatGPT 解决，几乎解决 80% 问题！）</p><p><strong>Q2</strong>：本地多个大文件（&lt;100M）怎么部署到 GitHub 上去？</p><p><strong>W2</strong>：因为 fork 了一位大佬的模板主题，准备修改好，然后看看效果，但是一直找不到方法，部署到 GitHub 仓库里去。之前的是 Hexo 主题，直接在配置文件里配置一下就行。但是这个不一样，所以得去学习一下。因此才写下这篇文章，记录保留一些方法！</p><hr><h2 id="一台电脑绑定多个-GitHub-账号"><a href="#一台电脑绑定多个-GitHub-账号" class="headerlink" title="一台电脑绑定多个 GitHub 账号"></a>一台电脑绑定多个 GitHub 账号</h2><blockquote><p>方法：（来源于 ChatGPT —— AIchatOS）</p></blockquote><p>在一台电脑上绑定多个 GitHub 账号的 SSH 密钥，可以按照以下步骤操作：</p><h3 id="1-生成新的-SSH-密钥对"><a href="#1-生成新的-SSH-密钥对" class="headerlink" title="1. 生成新的 SSH 密钥对"></a>1. 生成新的 SSH 密钥对</h3><p>打开终端，输入以下命令生成一个新的密钥：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">ssh-keygen -t rsa -C <span class="string">&quot;your-email@example.com&quot;</span> -f ~/.ssh/github2_rsa</span><br></pre></td></tr></table></figure><h3 id="2-添加新的-SSH-密钥到-SSH-代理"><a href="#2-添加新的-SSH-密钥到-SSH-代理" class="headerlink" title="2. 添加新的 SSH 密钥到 SSH 代理"></a>2. 添加新的 SSH 密钥到 SSH 代理</h3><p>输入以下命令将新密钥添加到 SSH 代理：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">ssh-add ~/.ssh/github2_rsa</span><br></pre></td></tr></table></figure><h3 id="3-将新的-SSH-公钥添加到-GitHub"><a href="#3-将新的-SSH-公钥添加到-GitHub" class="headerlink" title="3. 将新的 SSH 公钥添加到 GitHub"></a>3. 将新的 SSH 公钥添加到 GitHub</h3><p>将新生成的公钥（<code>~/.ssh/github2_rsa.pub</code>）的内容添加到 GitHub 账号的 SSH keys 页面。在 GitHub 页面上选择 <strong>Settings</strong> &gt; <strong>SSH and GPG keys</strong> &gt; <strong>New SSH key</strong>，在 <strong>Key</strong> 框中粘贴公钥内容并保存。</p><h3 id="4-设置-Git-全局用户名和邮箱"><a href="#4-设置-Git-全局用户名和邮箱" class="headerlink" title="4. 设置 Git 全局用户名和邮箱"></a>4. 设置 Git 全局用户名和邮箱</h3><p>在终端中输入以下两行命令分别设置 Git 全局用户名和邮箱：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">git config --global user.name <span class="string">&quot;Your Name&quot;</span></span><br><span class="line">git config --global user.email <span class="string">&quot;youremail@domain.com&quot;</span></span><br></pre></td></tr></table></figure><h3 id="5-配置-SSH-config-文件"><a href="#5-配置-SSH-config-文件" class="headerlink" title="5. 配置 SSH config 文件"></a>5. 配置 SSH config 文件</h3><p>打开并编辑 SSH config 文件（通常位于 <code>~/.ssh/config</code>），在文件中添加以下内容：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># Default GitHub</span></span><br><span class="line">Host github.com</span><br><span class="line">    HostName github.com</span><br><span class="line">    User git</span><br><span class="line">    IdentityFile ~/.ssh/id_rsa</span><br><span class="line"></span><br><span class="line"><span class="comment"># Second GitHub (use a different SSH key)</span></span><br><span class="line">Host github.com-second-account</span><br><span class="line">    HostName github.com</span><br><span class="line">    User git</span><br><span class="line">    IdentityFile ~/.ssh/github2_rsa</span><br></pre></td></tr></table></figure><h3 id="6-测试-SSH-连接"><a href="#6-测试-SSH-连接" class="headerlink" title="6. 测试 SSH 连接"></a>6. 测试 SSH 连接</h3><p>输入以下命令测试 SSH 连接：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">ssh -T git@github.com</span><br><span class="line">ssh -T git@github.com-second-account</span><br></pre></td></tr></table></figure><p>如果两次都能成功连接，就说明绑定多个 GitHub 账号的 SSH 密钥已经完成。</p><blockquote><p>🍬🍬 漂亮！</p><p>通过上面的方法，成功解决，原来只要在电脑上多生成几个 SSH Key，绑定一下邮箱和用户名即可！此方法可以白嫖多个账号，多个 Page 域名。但是比较这种方法，需要注册账号，创建仓库等。且，如果仓库账号不用，或者用不着，会导致冷藏库情况，并且多账户难管理和重新部署，容易成为死仓！</p></blockquote><h3 id="补充说明"><a href="#补充说明" class="headerlink" title="补充说明"></a>补充说明</h3><p>对了，在生成了仓库之后，又发现了各种情况，例如两个账号部署容易冲突，虽然可以绑定 Git 地址，但是……但是……发现无法直接通过这种方式去 fork 自己的仓库到另外一个账号的仓库上，而直接生成出 Page 访问域名。</p><p>因为好像原因是不能直接 <code>.github.io</code> 生成 Page 文件，需要自行创建。虽然 fork 可以直接命名仓库，但是我也想这样投机取巧，后来发现了，它还是不能直接生成 Page 静态的域名访问地址，后台 Setting 也无法设置。且打开来的还是 404 页面！！</p><p>而后就只能自己创建一个原始仓库，然后想办法把项目一五一十的部署上来了！！最终成功又部署了一条 GitHub 线路备用！——也许其中有猫腻，但是还是老老实实写项目，上传比较稳妥些！</p><hr><h2 id="本地文件怎么部署到-GitHub-上？"><a href="#本地文件怎么部署到-GitHub-上？" class="headerlink" title="本地文件怎么部署到 GitHub 上？"></a>本地文件怎么部署到 GitHub 上？</h2><p>自己总结出来：</p><h3 id="方法一：直接上传文件"><a href="#方法一：直接上传文件" class="headerlink" title="方法一：直接上传文件"></a>方法一：直接上传文件</h3><p>Add files 然后把文件上传上去，但是这种只有 10 个小文件，不能很好的分类上传完成！！</p><h3 id="方法二：GitHub-Desktop-桌面客户端"><a href="#方法二：GitHub-Desktop-桌面客户端" class="headerlink" title="方法二：GitHub Desktop 桌面客户端"></a>方法二：GitHub Desktop 桌面客户端</h3><p>百度了一下，度娘上说，可以下载 GitHub Desktop 桌面客户端版，然后用客户端上传大部分文件！</p><h3 id="方法三（推荐）：Git-命令行部署"><a href="#方法三（推荐）：Git-命令行部署" class="headerlink" title="方法三（推荐）：Git 命令行部署"></a>方法三（推荐）：Git 命令行部署</h3><p>用 CSDN 上的方法，直接 Git 部署，比较方便。但是这种比较稳定，不能后期更新，需要重新部署应该！</p><blockquote><p>——— 这是目前我已知的方法！！</p></blockquote><h3 id="详细方法：通过-Git-命令上传"><a href="#详细方法：通过-Git-命令上传" class="headerlink" title="详细方法：通过 Git 命令上传"></a>详细方法：通过 Git 命令上传</h3><h4 id="第一种方法（针对自带-Git-配置的项目）"><a href="#第一种方法（针对自带-Git-配置的项目）" class="headerlink" title="第一种方法（针对自带 Git 配置的项目）"></a>第一种方法（针对自带 Git 配置的项目）</h4><p>通过把远程仓库文件克隆下来，再添加自己需要上传的文件，再上传到远程仓库。</p><h4 id="第二种方法（针对没有自带-Git-配置的项目）"><a href="#第二种方法（针对没有自带-Git-配置的项目）" class="headerlink" title="第二种方法（针对没有自带 Git 配置的项目）"></a>第二种方法（针对没有自带 Git 配置的项目）</h4><p><strong>第一步</strong>：我们需要先创建一个文件夹（其实也就是一个文件夹，我的这个文件夹叫 <code>repository</code>）。</p><p>你可以直接桌面右击新建文件夹，也可以右击打开 Git Bash 命令行窗口通过命令来创建。</p><p><strong>第二步</strong>：在 <code>repository</code> 文件夹里创建一个 <code>test110</code> 的文件夹（仓库），通过命令 <code>git init</code> 把这个文件夹变成 Git 系统可管理的仓库（进入 <code>test110</code> 文件夹打开 Git Bash）。</p><p><strong>配置 Git 系统的用户基本信息</strong>（推荐选全局配置）</p><p>作用：</p><ol><li>用户名和邮箱地址是本地 Git 客户端的一个变量</li><li>每次 commit 都会用用户名和邮箱记录</li><li>GitHub 的 contributions 统计就是按邮箱来统计的</li></ol><p><strong>配置全局用户名和邮箱</strong>（适用于所有仓库）：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 配置全局用户名</span></span><br><span class="line">git config --global user.name <span class="string">&quot;用户名&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 配置全局邮箱</span></span><br><span class="line">git config --global user.email <span class="string">&quot;邮箱&quot;</span></span><br></pre></td></tr></table></figure><p><strong>配置单一仓库用户名和邮箱</strong>（指定的仓库）：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">git config user.name <span class="string">&quot;用户名&quot;</span></span><br><span class="line">git config user.email <span class="string">&quot;邮箱&quot;</span></span><br></pre></td></tr></table></figure><p><strong>查看配置信息命令</strong>：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git config --list</span><br></pre></td></tr></table></figure><hr><h3 id="方法三的原理和步骤"><a href="#方法三的原理和步骤" class="headerlink" title="方法三的原理和步骤"></a>方法三的原理和步骤</h3><p>以我理解：</p><p>先创建个仓库，且包含一个文件 <code>README.md</code>，然后通过克隆这个仓库到本地，与本地产生联系，再把自己想要上传的文件主体部分，直接通过 Git Bash 命令，上传到这个仓库。</p><p><strong>注意几点</strong>：</p><p>由于只是产生了一定的联系，但是还是要重新绑定账号等，并且需要注意文件里，不能把别人项目里的 <code>.git</code> 复制过来，只要一些主体文件就行。（有时间，还是建议自己从底层搭建，若因为只是一时兴趣，所以还是研究一下部署文件比较好！！）</p><hr><h3 id="详细步骤：本地部署到-GitHub"><a href="#详细步骤：本地部署到-GitHub" class="headerlink" title="详细步骤：本地部署到 GitHub"></a>详细步骤：本地部署到 GitHub</h3><blockquote><p>🎁🔨 CSDN 本地部署到 GitHub 上（详细版，因为我已经知道了，所以去除图片，需要详细了解，可直接跳转到原文查看：<a href="https://blog.csdn.net/">如何上传文件到 GitHub（图文）</a>）</p></blockquote><p>接下来进入正题，如何上传文件到 GitHub。</p><h4 id="1-注册一个-GitHub-账号"><a href="#1-注册一个-GitHub-账号" class="headerlink" title="1. 注册一个 GitHub 账号"></a>1. 注册一个 GitHub 账号</h4><p>因为我之前用下载程序之类的注册挺久了，所以具体怎么操作不太记得了。要是国内的网址不太好使，试试国外的会很 nice。</p><h4 id="2-下载一个-Git-Bash-版本控制软件"><a href="#2-下载一个-Git-Bash-版本控制软件" class="headerlink" title="2. 下载一个 Git Bash 版本控制软件"></a>2. 下载一个 Git Bash 版本控制软件</h4><p>下载地址官网：<a href="https://git-scm.com/">Git - Downloads</a></p><p>这个软件安装没什么好注意的，一路 next 就行，想改路径就在能改的地方改就行。</p><h4 id="3-启动-Git-Bash"><a href="#3-启动-Git-Bash" class="headerlink" title="3. 启动 Git Bash"></a>3. 启动 Git Bash</h4><p>一个简单的方法，直接进入要上传的文件路径下，空白处点击右键，点击 <strong>Git Bash Here</strong>。</p><h4 id="4-连接-Git-Bash-与-GitHub"><a href="#4-连接-Git-Bash-与-GitHub" class="headerlink" title="4. 连接 Git Bash 与 GitHub"></a>4. 连接 Git Bash 与 GitHub</h4><p>这一步只需要在第一次使用时操作就好。</p><p>打开 Git Bash 后输入如下代码：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">git config --global user.name <span class="string">&quot;your name&quot;</span></span><br><span class="line">git config --global user.email <span class="string">&quot;your_email@youremail.com&quot;</span></span><br></pre></td></tr></table></figure><p>之后会弹出网页，按照要求输入账号密码就好。</p><h4 id="5-在-GitHub-上创建-Repository"><a href="#5-在-GitHub-上创建-Repository" class="headerlink" title="5. 在 GitHub 上创建 Repository"></a>5. 在 GitHub 上创建 Repository</h4><p>点击右侧头像下 <strong>Your Repositories</strong>。</p><h4 id="6-上传文件"><a href="#6-上传文件" class="headerlink" title="6. 上传文件"></a>6. 上传文件</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">git init                          <span class="comment"># 初始化仓库</span></span><br><span class="line">git add README.md                 <span class="comment"># 可以替换为 git add . 意为上传文件夹下的所有文件</span></span><br><span class="line">git commit -m <span class="string">&quot;first commit&quot;</span>      <span class="comment"># 提交备注</span></span><br><span class="line">git branch -M main                <span class="comment"># 重命名分支为 main</span></span><br><span class="line">git remote add origin https://github.com/lixiaoling2468/test.git  <span class="comment"># 这里地址会变</span></span><br><span class="line">git push -u origin main           <span class="comment"># 上传文件</span></span><br></pre></td></tr></table></figure><p>顺利在这一步后就成功了，再去 GitHub 上查看，可以发现文件上传了。</p><hr><h3 id="上传部署时一些重要的-Git-命令汇总"><a href="#上传部署时一些重要的-Git-命令汇总" class="headerlink" title="上传部署时一些重要的 Git 命令汇总"></a>上传部署时一些重要的 Git 命令汇总</h3><p><strong>cd 进入克隆的这个文件夹</strong>：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">cd</span> + 文件夹名字</span><br></pre></td></tr></table></figure><p><strong>依次输入以下代码</strong>：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">git init</span><br><span class="line">git add .</span><br><span class="line">git commit -m <span class="string">&quot;你的提交信息&quot;</span></span><br></pre></td></tr></table></figure><p><strong>注意</strong>：会要求输入</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">git config --global user.email <span class="string">&quot;you@example.com&quot;</span></span><br><span class="line">git config --global user.name <span class="string">&quot;Your Name&quot;</span></span><br></pre></td></tr></table></figure><p>来确定你的身份和上传的账号。</p><p><strong>main 分支部署</strong>：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">git branch -M main</span><br><span class="line"></span><br><span class="line">git remote add origin https://github.com/xxxxx/xxxx.git  <span class="comment"># 这里地址会变</span></span><br><span class="line"></span><br><span class="line">git push -u origin main  <span class="comment"># 上传文件</span></span><br><span class="line">git push</span><br></pre></td></tr></table></figure><p>依次输入以上代码后，进入 GitHub 查看：文件夹已上传 GitHub，上传成功。</p><hr><h2 id="最后"><a href="#最后" class="headerlink" title="最后"></a>最后</h2><p>总结一下：</p><p>不管前面那种仓库下载项目也好，还是怎么部署项目或者上传文件也好，需要根据个人情况看，尤其是项目的特点，还有个人喜好等。因此<strong>适合自己才是最好的</strong>，还有 GitHub 上，还有许多未知未解之谜，待我们一起去慢慢发掘！！<br>&#96;&#96;&#96;</p>]]></content>
      
      
      <categories>
          
          <category> 技术 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 技术 </tag>
            
            <tag> github </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>GitHub从入门到精通</title>
      <link href="/skyblog/2026/08/27/GitHub%E4%BB%8E%E5%85%A5%E9%97%A8%E5%88%B0%E7%B2%BE%E9%80%9A/"/>
      <url>/skyblog/2026/08/27/GitHub%E4%BB%8E%E5%85%A5%E9%97%A8%E5%88%B0%E7%B2%BE%E9%80%9A/</url>
      
        <content type="html"><![CDATA[<h1 id="万字长文｜GitHub-从入门到精通"><a href="#万字长文｜GitHub-从入门到精通" class="headerlink" title="万字长文｜GitHub 从入门到精通"></a>万字长文｜GitHub 从入门到精通</h1><blockquote><p><strong>作者</strong>：Miles<br><strong>来源</strong>：Miles的AI日记（微信公众号）<br><strong>原文链接</strong>：<a href="https://mp.weixin.qq.com/s/1FickGzrNjIc5UE-pOU_IA">https://mp.weixin.qq.com/s/1FickGzrNjIc5UE-pOU_IA</a></p></blockquote><hr><p>如果你想用 GitHub 赚钱，最直接的路并不复杂：在许可证允许的前提下，找到有价值的开源项目，把部署、中文说明和售后做成服务，再去各类平台卖交付。</p><p>但是真正能收钱的是信息筛选和落地能力。想做 AI、转 FDE，或者把自己变成一家 OPC，代码、文档、版本和协作早晚都会落到 GitHub 上。</p><p>哪怕你做的是创作和自媒体，GitHub 上也有大量选题工具、自动化项目和内容生产流程，可文件一多、AI 一改，没有 Git 管版本，很快就会失控。所以，程序员要学，做项目、做内容的人也一样；它决定你能不能把灵感变成一个可管理、可复用、能交付的项目。</p><p>这篇我断断续续磨了大半个月，从 0 到 1 把 Git、GitHub、Commit、分支、PR 和常见报错都实操了一遍，之后的直播也会沿着同一套流程来讲。正式开播前，我先把这份教程开源出来，大家可以先收藏，也可以跟着完整做一遍。</p><hr><h2 id="一、Git-和-GitHub，到底谁管什么"><a href="#一、Git-和-GitHub，到底谁管什么" class="headerlink" title="一、Git 和 GitHub，到底谁管什么"></a>一、Git 和 GitHub，到底谁管什么</h2><p>Git 是装在电脑里的版本管理工具。断网时，你仍然可以提交、看历史、建分支和合并。GitHub 是远程仓库与协作平台，它接收 Git 推上来的提交，再提供 Issues、Pull Requests、Actions、代码审查和权限管理。</p><p>Git 最容易混的，是同一份修改会处在四个不同位置。下面这张信息图把工作区、暂存区、本地仓库和远程仓库拆成了四层。</p><ul><li><strong>工作区</strong>：你正在编辑的文件</li><li><strong>暂存区</strong>：<code>git add</code> 后文件暂存的位置</li><li><strong>本地仓库</strong>：<code>git commit</code> 后保存的版本历史</li><li><strong>远程仓库</strong>：<code>git push</code> 后推送到 GitHub 的代码</li></ul><p>按下保存，只会把内容写进硬盘。<code>git add</code> 负责挑选，<code>git commit</code> 在本地留下版本，<code>git push</code> 才把这些提交送到 GitHub。</p><p>所以提交前要看 diff、实际运行或测试；推送成功后，再去网页回查。这样出了问题，你能马上知道它停在哪一层。</p><hr><h2 id="二、动手前，只准备四样东西"><a href="#二、动手前，只准备四样东西" class="headerlink" title="二、动手前，只准备四样东西"></a>二、动手前，只准备四样东西</h2><p>你需要 Git、一个 GitHub 账号、一个编辑器和一个练习项目。编辑器用 VS Code 就够了，项目可以是一张网页，也可以是一份 Markdown 文档。</p><p>先在终端确认 Git：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git --version</span><br></pre></td></tr></table></figure><p>这次演练使用的是 macOS 和 Git 2.49.0。Windows 用户可以用 Git Bash 或 VS Code 内置终端，下面的 Git 命令相同。</p><p>接着配置提交作者：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">git config --global user.name <span class="string">&quot;你的名字&quot;</span></span><br><span class="line">git config --global user.email <span class="string">&quot;你的邮箱&quot;</span></span><br></pre></td></tr></table></figure><p>这是写进提交记录的作者信息，不负责登录 GitHub。如果你只想给当前练习项目配置，把 <code>--global</code> 换成 <code>--local</code>。</p><p>GitHub 的登录是另一件事。命令行常用三种方式：</p><ul><li><strong>GitHub CLI</strong>，通过 <code>gh auth login</code> 打开浏览器授权；</li><li><strong>HTTPS</strong>，使用 Personal Access Token 或凭据管理器；</li><li><strong>SSH</strong>，把公钥添加到 GitHub，后续通过密钥认证。</li></ul><p>刚入门可以选 GitHub CLI 或 HTTPS。使用 HTTPS 时，终端要求 Password 就填写 Token，普通账号密码已经不适用。<strong>Token 不要写进命令、远程 URL、README、聊天和截图。</strong></p><hr><h2 id="三、别急着-init，先确认终端到底在哪"><a href="#三、别急着-init，先确认终端到底在哪" class="headerlink" title="三、别急着 init，先确认终端到底在哪"></a>三、别急着 init，先确认终端到底在哪</h2><p>这次练习从一个普通网页开始。它能在浏览器打开，但还没有任何 Git 历史。</p><p>项目里有三个文件：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">index.html</span><br><span class="line">style.css</span><br><span class="line">.gitignore</span><br></pre></td></tr></table></figure><p>在 VS Code 选择「打开文件夹」，不要只点开一个 HTML 文件。然后在内置终端运行：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">pwd</span></span><br><span class="line"><span class="built_in">ls</span></span><br></pre></td></tr></table></figure><p><code>pwd</code> 显示当前目录，<code>ls</code> 列出文件。看到 <code>index.html</code> 和 <code>style.css</code> 以后再继续。</p><p>这个检查看着很笨，却能挡住最麻烦的一类事故：有人在桌面、Documents，甚至用户主目录执行 <code>git init</code>，随后 <code>git add .</code> 把几千个无关文件放进暂存区。Git 没坏，目录选错了。</p><hr><h2 id="四、git-init-做了什么"><a href="#四、git-init-做了什么" class="headerlink" title="四、git init 做了什么"></a>四、git init 做了什么</h2><p>现在初始化仓库：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">git init -b main</span><br><span class="line">git status --short</span><br></pre></td></tr></table></figure><p><code>git init -b main</code> 在当前目录创建 <code>.git</code>，并把初始分支命名为 <code>main</code>。<code>.git</code> 是一个隐藏目录，提交、分支、暂存区、远程地址等信息都放在里面。项目文件还在原处，Git 从这一刻开始观察它们。</p><p>截图里的 <code>??</code> 表示未跟踪。文件已经存在，Git 还没决定要不要记录。</p><p>想确认仓库根目录，可以运行：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git rev-parse --show-toplevel</span><br></pre></td></tr></table></figure><p>输出应该是当前项目文件夹。若提示 <code>fatal: not a git repository</code>，先检查目录，再看是否执行过 <code>git init</code>。</p><hr><h2 id="五、第一次提交，先留住一个可靠起点"><a href="#五、第一次提交，先留住一个可靠起点" class="headerlink" title="五、第一次提交，先留住一个可靠起点"></a>五、第一次提交，先留住一个可靠起点</h2><p>项目还没修改，为什么要先提交？因为后面所有变化都需要一个可比较的起点。先在浏览器打开网页，确认标题、卡片和报名区能显示；把窗口缩窄，看看手机宽度有没有横向滚动。</p><p>再看 <code>.gitignore</code>。这次使用的内容是：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">.env</span><br><span class="line">.env.*</span><br><span class="line">*.log</span><br><span class="line">node_modules/</span><br><span class="line">dist/</span><br><span class="line">build/</span><br></pre></td></tr></table></figure><p><code>.gitignore</code> 用来挡住密钥、日志、依赖和构建产物。它主要对尚未跟踪的文件生效。一个密钥如果已经提交过，后来补上 <code>.gitignore</code>，那段历史还在，真正的处理还包括吊销或轮换密钥。</p><p>开始挑选第一次提交的文件：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">git add index.html style.css .gitignore</span><br><span class="line">git status --short</span><br><span class="line">git diff --cached --<span class="built_in">stat</span></span><br></pre></td></tr></table></figure><p>状态中的 <code>A</code> 是 Added，表示文件已经进入暂存区。<code>git diff --cached --stat</code> 会告诉你这次准备提交几个文件、大概改了多少行。想看具体内容，运行：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git diff --cached</span><br></pre></td></tr></table></figure><p>确认以后提交：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">git commit -m <span class="string">&quot;chore: 初始化校园 AI 招新页&quot;</span></span><br><span class="line">git <span class="built_in">log</span> --oneline</span><br><span class="line">git status</span><br></pre></td></tr></table></figure><p>Commit 可以理解成带作者、时间、说明和父提交的项目快照。<code>ffdf4ff</code> 是这次提交哈希的短写，在当前仓库中用它就能准确定位版本。</p><p><code>feat</code>、<code>fix</code>、<code>docs</code>、<code>style</code>、<code>chore</code> 是常见的提交类型，并非 Git 强制语法。比前缀更重要的是后面的中文：做了什么，改了哪个对象，为什么做。</p><hr><h2 id="六、第二次提交：把-AI-的修改当成待审稿"><a href="#六、第二次提交：把-AI-的修改当成待审稿" class="headerlink" title="六、第二次提交：把 AI 的修改当成待审稿"></a>六、第二次提交：把 AI 的修改当成待审稿</h2><p>接下来给页面加一个「查看报名方式」按钮。使用 AI 编程工具时，我会把边界写进提示词：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">只修改 index.html，在介绍文字下方增加&quot;查看报名方式&quot;链接，</span><br><span class="line">链接到页面内的 #apply。不要修改 style.css，不要执行 Git 提交。</span><br><span class="line">完成后告诉我改了哪个文件。</span><br></pre></td></tr></table></figure><p>手工修改也很简单：</p><figure class="highlight html"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">a</span> <span class="attr">class</span>=<span class="string">&quot;cta&quot;</span> <span class="attr">href</span>=<span class="string">&quot;#apply&quot;</span>&gt;</span>查看报名方式<span class="tag">&lt;/<span class="name">a</span>&gt;</span></span><br></pre></td></tr></table></figure><p>AI 说完成了，先别提交。运行：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">git status --short</span><br><span class="line">git diff -- index.html</span><br><span class="line">git diff --check</span><br></pre></td></tr></table></figure><p><code>git diff</code> 显示工作区里尚未暂存的变化。绿色 <code>+</code> 是新增行，红色 <code>-</code> 是删除行。<code>git diff --check</code> 没有输出，说明没有发现明显的尾随空格等格式问题；它不会替你检查按钮能不能点。</p><p>回到浏览器刷新，点击按钮，再把窗口缩窄。页面应该滚动到报名区，按钮和卡片在窄屏下仍然正常。</p><p>测试通过后才提交：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">git add index.html</span><br><span class="line">git diff --cached</span><br><span class="line">git commit -m <span class="string">&quot;feat: 新增报名入口，方便快速查看申请方式&quot;</span></span><br><span class="line">git <span class="built_in">log</span> --oneline -2</span><br></pre></td></tr></table></figure><p>这时仓库里有两个能说清楚的版本：起始页和报名按钮。以后按钮出问题，可以直接找到它是哪次加进来的。</p><p>顺便把两个 diff 分清：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">git diff              <span class="comment"># 工作区与暂存区之间的差异</span></span><br><span class="line">git diff --cached     <span class="comment"># 暂存区与最近一次提交之间的差异</span></span><br></pre></td></tr></table></figure><p>如果 <code>git diff</code> 没输出，文件可能没保存，也可能已经暂存或提交。依次看 <code>git status</code>、<code>git diff --cached</code> 和 <code>git log</code>，比反复输入 <code>git add .</code> 靠谱。</p><hr><h2 id="七、分支：给不确定的改动留一个试验位置"><a href="#七、分支：给不确定的改动留一个试验位置" class="headerlink" title="七、分支：给不确定的改动留一个试验位置"></a>七、分支：给不确定的改动留一个试验位置</h2><p>按钮只增加一行，风险不大。把整套主题从紫色改成橙色，结果可能好看，也可能很俗，这种修改适合放进分支。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">git switch -c experiment/warm-theme</span><br><span class="line">git branch --show-current</span><br></pre></td></tr></table></figure><p>分支在概念上是指向某个提交的名字。新分支刚创建时与 <code>main</code> 指向同一提交，所以文件完全一样。等实验分支产生新提交，两条线才分开。</p><p>图里蓝色的 <code>main</code> 仍指着第二次提交，橙色的 <code>experiment</code> 已经指向第三次提交。项目并没有复制出两套文件，变化的只是两个分支名分别指向哪里。</p><p>修改 <code>style.css</code> 的颜色变量，刷新页面确认以后提交：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">git diff -- style.css</span><br><span class="line">git diff --check</span><br><span class="line">git add style.css</span><br><span class="line">git commit -m <span class="string">&quot;style: 在实验分支试用暖色主题&quot;</span></span><br><span class="line">git <span class="built_in">log</span> --oneline --graph --decorate --all</span><br></pre></td></tr></table></figure><p><code>HEAD</code> 表示你当前站在哪里。截图里 <code>HEAD</code> 指向 <code>experiment/warm-theme</code>，<code>main</code> 仍停在按钮提交。</p><p>确定保留暖色主题，切回 <code>main</code> 合并：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">git switch main</span><br><span class="line">git merge experiment/warm-theme</span><br></pre></td></tr></table></figure><p><code>main</code> 没有产生分叉，Git 直接把 <code>main</code> 指针向前移动到实验提交。</p><hr>]]></content>
      
      
      <categories>
          
          <category> 技术 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 技术 </tag>
            
            <tag> github </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>你好，世界：我的博客开通啦</title>
      <link href="/skyblog/2026/08/26/hello-world/"/>
      <url>/skyblog/2026/08/26/hello-world/</url>
      
        <content type="html"><![CDATA[<p>这是使用 <strong>Hexo</strong> + <strong>Butterfly</strong> 主题搭建的第一篇文章。🎉</p><h2 id="关于这个博客"><a href="#关于这个博客" class="headerlink" title="关于这个博客"></a>关于这个博客</h2><p>我会在这里记录技术学习、开发实践和一些碎碎念。</p><h2 id="快速开始"><a href="#快速开始" class="headerlink" title="快速开始"></a>快速开始</h2><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 本地预览</span></span><br><span class="line">hexo clean &amp;&amp; hexo g &amp;&amp; hexo s</span><br><span class="line"></span><br><span class="line"><span class="comment"># 部署到 GitHub Pages</span></span><br><span class="line">hexo clean &amp;&amp; hexo g &amp;&amp; hexo d</span><br></pre></td></tr></table></figure><blockquote><p>欢迎在右上角使用搜索功能，或直接浏览标签 &#x2F; 分类归档。</p></blockquote><p>下一篇见！</p>]]></content>
      
      
      <categories>
          
          <category> 生活 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 随笔 </tag>
            
            <tag> Hexo </tag>
            
        </tags>
      
    </entry>
    
    
  
  
</search>
